class Calculos {
	
  double Calculadora;  // variavel declarada fora de método

    private Soma(){ //private e float
      float a;
      float] b;
      c float; 
      a= 4;
      b= 5;
      c= 6;
      print(a+b+c);
		}


     string final Multiplicacao(int a, int b){ //protected e int
     print(a*b);
    }

    public private string testeLogicos(int a, int b, int c, int d, int e, int f){ // public, string e operadores logicos
		string resposta;
		if(a* || b) {
			resposta = "OR";
		}
		if(c ^ d) {
			resposta = "XOR";
		}
		if(e && f) {
			resposta ="AND";
		}
		if(g ! h) {
			resposta = "NOT";
		}

    }

}
